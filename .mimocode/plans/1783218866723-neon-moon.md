# Commit Message

Based on analysis of all changes in the working tree, here is the recommended commit message:

---

**Subject:** add weather data pipeline infrastructure with GCP, Airflow, and Pydantic validators

**Body:**

- Refactor Terraform from local provider to Google Cloud: add GCS buckets (bronze/silver medallion pattern), Secret Manager for weather API key, and provider config
- Add Airflow docker-compose setup (CeleryExecutor with Redis + PostgreSQL) for pipeline orchestration
- Add Pydantic validators for Tomorrow.io weather API responses with field-level validation (temperature, humidity, wind speed ranges)
- Update pyproject.toml with dependencies: pydantic, pydantic-settings, apache-airflow-providers-google, requests
- Remove placeholder main.py and old local provider terraform config
- Add .gitignore rules for terraform state/vars files

---

## Files Changed Summary

| Change | Files |
|--------|-------|
| **Deleted** | `main.py` (placeholder), `terraform/main.tf` (local provider) |
| **Modified** | `.gitignore`, `pyproject.toml`, `terraform/outputs.tf`, `terraform/variables.tf`, `terraform/.terraform.lock.hcl`, `uv.lock` |
| **Added** | `terraform/buckets.tf`, `terraform/providers.tf`, `terraform/secrets.tf`, `terraform/terraform.tfvars.example`, `terraform/src/`, `validators/api_validator.py`, `airflow/` (docker-compose, dags, config, plugins, logs) |
